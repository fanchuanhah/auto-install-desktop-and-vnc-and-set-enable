# 用于给ubuntu/debian自动安装桌面环境和vnc服务，并配置开机自启动

安装方式
```bash
bash <(curl -L sh.802213.xyz/zm/install.sh)
```
或
```bash <(curl -L https://github.com/fanchuanhah/auto-install-desktop-and-vnc-and-set-enable/raw/refs/heads/main/install.sh)
bash
```
